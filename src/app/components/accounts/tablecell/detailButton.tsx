import { Button } from "@mui/material"


export default function DetailButton(){
    return (
        <Button sx={{ fontFamily: ""}}>
            <b>{'Details'}   &nbsp;  {'>'}</b>
        </Button>
    )
}